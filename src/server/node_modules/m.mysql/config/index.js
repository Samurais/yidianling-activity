
// 基础表
const activityBase = require("../table/activityBase");

module.exports = {

  // 数据库名
  databaseName: "node_activity",

  // 表名列表
  tableName: [
    // 基础表
    {
      name: "activity_base",
    },
    // 布局表
    {
      name: "activity_layout",
    }
  ],

  // 表对应的字段名字
  tableFields: {

    // 基础表字段
    activity_base: { ...activityBase},

    // 布局表字段
    activity_layout: {
      //
    }
  }
}
